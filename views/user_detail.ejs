<!DOCTYPE html>

<html lang="en" class="material-style layout-fixed">

<head>
    <title>Detail</title>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0">
    <meta name="description"
        content="Bhumlu Bootstrap admin template made using Bootstrap 4, it has tons of ready made feature, UI components, pages which completely fulfills any dashboard needs." />
    <meta name="keywords"
        content="Bhumlu, bootstrap admin template, bootstrap admin panel, bootstrap 4 admin template, admin template">
    <meta name="author" content="Srthemesvilla" />
    <link rel="icon" type="image/x-icon" href="/assets/img/favicon.ico">

    <!-- Google fonts -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
  

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap4.min.css">
    <link rel="stylesheet" href="https://cdn.datatables.net/responsive/2.2.9/css/responsive.bootstrap4.min.css">

    <!-- Icon fonts -->
    <link rel="stylesheet" href="/assets/fonts/fontawesome.css">
    <link rel="stylesheet" href="/assets/fonts/ionicons.css">
    <link rel="stylesheet" href="/assets/fonts/linearicons.css">
    <link rel="stylesheet" href="/assets/fonts/open-iconic.css">
    <link rel="stylesheet" href="/assets/fonts/pe-icon-7-stroke.css">
    <link rel="stylesheet" href="/assets/fonts/feather.css">

    <!-- Core stylesheets -->
    <link rel="stylesheet" href="/assets/css/bootstrap-material.css">
    <link rel="stylesheet" href="/assets/css/shreerang-material.css">
    <link rel="stylesheet" href="/assets/css/uikit.css">

    <!-- Libs -->
    <link rel="stylesheet" href="/assets/libs/perfect-scrollbar/perfect-scrollbar.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
    <style>
   /* Container for profile section */
   .profile-card {
    background: #fff;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
}

.profile-item {
    margin-bottom: 15px;
}

.profile-item input {
    border-radius: 8px;
    border: 1px solid #ced4da;
    padding: 10px;
    padding-left: 12px !important;
    transition: all 0.3s ease;
}

.profile-item input:focus {
    border-color: #007bff;
    box-shadow: 0px 0px 5px rgba(0, 123, 255, 0.5);
    outline: none;
}

.disabled-input {
    background: #f8f9fa;
    cursor: not-allowed;
}

/* Light-colored button */
.btn-update {
    background: #69a3dc;
    color: #050505;
    border: 1px solid #007bff;
    transition: all 0.3s ease;
}

.btn-update:hover {
    background: #007bff;
    color: #fff;
}


    </style>
</head>

<body>
    <!-- [ Preloader ] Start -->
    <div class="page-loader">
        <div class="bg-primary"></div>
    </div>
    <!-- [ Preloader ] Ebd -->

    <!-- [ Layout wrapper ] Start -->
    <div class="layout-wrapper layout-2">
        <div class="layout-inner">
            <!-- [ Layout sidenav ] Start -->
            <div id="layout-sidenav" class="layout-sidenav sidenav sidenav-vertical bg-dark">
                <!-- Brand demo (see assets/css/demo/demo.css) -->
                <div class="app-brand demo">
                    <span class="app-brand-logo demo">
                        <img src="/assets/img/logo.png" alt="Brand Logo" class="img-fluid">
                    </span>
                    <a href="" class="app-brand-text demo sidenav-text font-weight-normal ml-2">Admin</a>
                    <a href="javascript:" class="layout-sidenav-toggle sidenav-link text-large ml-auto">
                        <i class="ion ion-md-menu align-middle"></i>
                    </a>
                </div>
                <div class="sidenav-divider mt-0"></div>

                <!-- Links -->
                <ul class="sidenav-inner py-1">



                    <!-- Layouts -->


                    <!-- UI elements -->


                    <!-- Forms & Tables -->
                    <li class="sidenav-divider mb-1"></li>
                    <li class="sidenav-header small font-weight-semibold"></li>

                    <li class="sidenav-item active">
                        <a href="/users/tables" class="sidenav-link">
                            <i class="sidenav-icon feather icon-grid"></i>
                            <div>Tables</div>
                        </a>
                    </li>
                    <li class="sidenav-divider mb-1"></li>
                    <li class="sidenav-item">
                        <a class="sidenav-link">
                            <i class="sidenav-icon feather icon-lock"></i>
                            <button class="logout"
                                style="background-color:  rgba(29, 34, 83, 0.15); color: #a5a5a5; border: none;">Log
                                Out</button>
                        </a>
                    </li>

                    <!--  Icons -->


                    <!-- Pages -->



                </ul>
            </div>
            <!-- [ Layout sidenav ] End -->
            <!-- [ Layout container ] Start -->
            <div class="layout-container">
                <!-- [ Layout navbar ( Header ) ] Start -->


                <!-- Brand demo (see assets/css/demo/demo.css) -->
                <a  class="navbar-brand app-brand demo d-lg-none py-0 mr-4">
                    <span class="app-brand-logo demo">
                        <img src="/assets/img/logo-dark.png" alt="Brand Logo" class="img-fluid">
                    </span>
                    <span class="app-brand-text demo font-weight-normal ml-2">Bhumlu</span>
                </a>

                <!-- Sidenav toggle (see assets/css/demo/demo.css) -->
                <div class="layout-sidenav-toggle navbar-nav d-lg-none align-items-lg-center mr-auto">
                    <a class="nav-item nav-link px-0 mr-lg-4" href="javascript:">
                        <i class="ion ion-md-menu text-large align-middle"></i>
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-toggle="collapse"
                    data-target="#layout-navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="navbar-collapse collapse" id="layout-navbar-collapse">
                    <!-- Divider -->
                    <hr class="d-lg-none w-100 my-2">



                </div>
                </nav>
                <!-- [ Layout navbar ( Header ) ] End -->

                <!-- [ Layout content ] Start -->


                <!-- [ content ] Start -->
                <div class="container-fluid flex-grow-1 container-p-y">

                    <div class="text-muted small mt-0 mb-4 d-block breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#"><i class="feather icon-home"></i></a></li>
                            <li class="breadcrumb-item">User Data</li>

                        </ol>
                    </div><br>


                    
                        <div class="container profile-container mt-6">

                            
                            <div class="profile-card p-4 shadow-lg rounded bg-white">
                                <% users.forEach(user => { %>
                                <h4 class="text-center text-primary mb-4">👤 User Detail</h4>
                                <form>
                                    <div class="row g-3">
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Name:</label>
                                            <input type="text" name="name" class="form-control rounded-3 shadow-sm" id="name"
                                                value="<%= user.name %>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Email:</label>
                                            <input type="email" name="email" class="form-control rounded-3 shadow-sm" id="email"
                                                value="<%= user.email%>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Role:</label>
                                            <input type="text" name="phone_number" class="form-control rounded-3 shadow-sm" id="phone"
                                                value="<%= user.role%>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Assign:</label>
                                            <input type="text" name="facebook" class="form-control rounded-3 shadow-sm" id="facebook"
                                                value="<%= user.assign %>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Status:</label>
                                            <input type="text" name="twitter" class="form-control rounded-3 shadow-sm" id="twitter"
                                                value="<%= user.status %>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Created At:</label>
                                            <input type="text" name="twitter" class="form-control rounded-3 shadow-sm" id="createdAt"
                                                value="<%= user.createdAt %>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Updated At:</label>
                                            <input type="text" name="twitter" class="form-control rounded-3 shadow-sm" id="updatedAt"
                                                value="<%= user.updatedAt %>">
                                        </div>
                                        <div class="col-md-6 profile-item">
                                            <label class="form-label fw-bold">Password:</label>
                                            <input type="text" name="twitter" class="form-control rounded-3 shadow-sm" id="password"
                                                value="<%= user.password %>">
                                        </div>
                                    </div>
                            
                                    <div class="text-center mt-4">
                                        <button type="button" data-id="<%= user.id %>"
                                            class="btn btn-success btn-update px-4 py-2 rounded-3 shadow-">
                                            Update
                                        </button>
                                    </div>
                                </form>
                                <% }) %>
                            </div>
                            
                                    <!-- Email Section -->

<br>

                                    <!-- <div id="emailFormSection">
                                        <div class="card">
                                            <h3 class="text-center text-dark fw-bold">✉️ Compose Email</h3>
                                           
                                            
                                            <form class="emailform">
                                                <div class="mb-3">
                                                    <label class="form-label">To</label>
                                                    <input type="email" class="form-control" name="ftoemail"
                                                        placeholder="Enter recipient email" required>
                                                </div>
                                    
                                                <div class="row">
                                                    <div class="col-md-6">
                                                        <label class="form-label">Cc</label>
                                                        <input type="text" class="form-control" name="fccemail"
                                                            placeholder="Enter CC email (optional)">
                                                    </div>
                                                </div>
                                    
                                                <div class="mb-3 mt-3">
                                                    <label class="form-label">Subject</label>
                                                    <input type="text" class="form-control" name="fsubject"
                                                        placeholder="Enter subject" required>
                                                </div>
                                    
                                                <div class="mb-3">
                                                    <label for="editor" class="form-label">Message:</label>
                                                    <div id="editor" style="height: 200px;"></div>
                                                    <input type="hidden" name="ftextarea" id="hiddenMessage">
                                                     -->
                                                        <!-- <label class="form-label"></label> -->
                                                        <!-- <input type="file" class="form-control" name="fattachment" id="fattachment" multiple>
                                                    
                                                </div>
                                    
                                                <div class="text-end">
                                                    <div class="btn-group">
                                                        <button type="submit" id="emailbtn" class=" btn-success">📩 Send</button>
                                                    </div>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                     -->
                                   
                        </div>
                        <!-- [ content ] End -->

                        <!-- [ Layout footer ] Start -->
                        <!-- <nav class="layout-footer footer bg-white">
                        <div class="container-fluid d-flex flex-wrap justify-content-between text-center container-p-x pb-3">
                            <div class="pt-3">
                                <span class="footer-text font-weight-semibold">&copy; <a href="https://srthemesvilla.com" class="footer-link" target="_blank">Srthemesvilla</a></span>
                            </div>
                            <div>
                                <a href="javascript:" class="footer-link pt-3">About Us</a>
                                <a href="javascript:" class="footer-link pt-3 ml-6">Help</a>
                                <a href="javascript:" class="footer-link pt-3 ml-4">Contact</a>
                                <a href="javascript:" class="footer-link pt-3 ml-4">Terms &amp; Conditions</a>
                            </div>
                        </div>
                    </nav> -->
                        <!-- [ Layout footer ] End -->

                    
                    <!-- [ Layout content ] Start -->

                </div>
                <!-- [ Layout container ] End -->

            </div>

            <!-- Overlay -->
            <div class="layout-overlay layout-sidenav-toggle"></div>
        </div>
        <!-- [ Layout wrapper ] end -->

        <!-- Core scripts -->
        <script src="/assets/js/pace.js"></script>
        <script src="/assets/js/jquery-3.3.1.min.js"></script>
        <script src="/assets/libs/popper/popper.js"></script>
        <script src="/assets/js/bootstrap.js"></script>
        <script src="/assets/js/sidenav.js"></script>
        <script src="/assets/js/layout-helpers.js"></script>
        <script src="/assets/js/material-ripple.js"></script>

        <!-- Libs -->
        <script src="/assets/libs/perfect-scrollbar/perfect-scrollbar.js"></script>

        <!-- Demo -->
        <script src="/assets/js/demo.js"></script>
        <script src="/assets/js/analytics.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
<script>

    document.addEventListener("click", function (event) {
        if (!event.target.classList.contains("logout")) return;
        event.stopPropagation();

        Swal.fire({
            title: "Are you sure?",
            text: "You will be logged out!",
            icon: "warning",
            showCancelButton: true,
            confirmButtonText: "Yes, logout!"
        }).then((result) => {
            if (!result.isConfirmed) return;

            Swal.fire({ title: "Logging out...", allowOutsideClick: false, showConfirmButton: false, didOpen: () => Swal.showLoading() });

            fetch("/users/logout", { method: "POST", headers: { "Content-Type": "application/json" } })
                .then((response) => {
                    if (!response.ok) throw new Error("Logout failed");
                    return response.json();
                })
                .then(() => {
                    Swal.fire("Logged out!", "You have been logged out successfully.", "success")
                        .then(() => (window.location.href = "/users/login"));
                })
                .catch((error) => {
                    console.error("Logout Error:", error);
                    Swal.fire("Error!", "An error occurred during logout.", "error");
                });
        });
    });



    document.addEventListener("click", function (event) {
        if (event.target.classList.contains("btn")) {
            const id = event.target.dataset.id;
            console.log("Update button clicked - ID:", id);

            if (!id) {
                console.error("Error: No ID found on button.");
                Swal.fire({ icon: "error", text: "Invalid item ID." });
                return;
            }

            // Gather form data
            const formData = {};
            document.querySelectorAll("input").forEach(input => {
                formData[input.name] = input.value;
            });
            formData.id = id;

            console.log("Data being sent:", formData); // Debugging

            Swal.fire({
                title: "Do you want to update?",
                showCancelButton: true,
                confirmButtonText: "Update",
            }).then((result) => {
                if (result.isConfirmed) {
                    fetch("/users/userupdate", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json"
                        },
                        body: JSON.stringify(formData)
                    })
                        .then(response => response.json())
                        .then(data => {
                            console.log("Server Response:", data);
                            if (data.status === "unauthorized") {
                                window.location.href = "/users/login";
                            } else if (data.status === "success") {
                                Swal.fire({
                                    icon: "success",
                                    text: data.message,
                                    confirmButtonText: "OK",
                                }).then((result) => {
                                    if (result.isConfirmed) {
                                        window.location.href = "/users/users_list";
                                    }
                                });
                            } else {
                                Swal.fire({
                                    icon: "error",
                                    title: "Oops...",
                                    text: data.message,
                                });
                            }
                        })
                        .catch(error => {
                            console.error("Fetch Error:", error);
                            Swal.fire({
                                icon: "error",
                                title: "Oops...",
                                text: "Something went wrong. Please try again later.",
                            });
                        });
                }
            });
        }
    });



    document.addEventListener("DOMContentLoaded", function () {
    const emailBtn = document.querySelector("#emailbtn");
    const quillEditor = new Quill("#editor", { theme: "snow" });

    if (emailBtn) {
        emailBtn.addEventListener("click", function (e) {
            e.preventDefault();

            Swal.fire({
                title: "Are you sure?",
                text: "This action cannot be undone!",
                icon: "warning",
                showCancelButton: true,
                confirmButtonText: "Send",
                cancelButtonText: "Cancel"
            }).then(async (result) => {
                if (result.isConfirmed) {
                    const formData = new FormData();
                    formData.append("ftoemail", document.querySelector("input[name='ftoemail']").value.trim());
                    formData.append("fccemail", document.querySelector("input[name='fccemail']").value.trim());
                    formData.append("fsubject", document.querySelector("input[name='fsubject']").value.trim());
                    formData.append("ftextarea", quillEditor.root.innerHTML.trim());

                    // Get file input and append files
                    const fileInput = document.querySelector("#fattachment");
                    if (fileInput.files.length > 0) {
                        for (let i = 0; i < fileInput.files.length; i++) {
                            formData.append("attachments", fileInput.files[i]);
                        }
                    }

                    try {
                        const response = await fetch("/users/sendEmail", {
                            method: "POST",
                            body: formData
                        });

                        const data = await response.json();

                        if (response.ok && data.status === "success") {
                            Swal.fire({
                                icon: "success",
                                text: "Email sent successfully!"
                            }).then(() => {
                                location.reload();
                            });
                        } else {
                            throw new Error(data.message || "Email sending failed.");
                        }
                    } catch (error) {
                        console.error("Email Error:", error);
                        Swal.fire({
                            icon: "error",
                            title: "Oops...",
                            text: "Something went wrong. Please try again later."
                        });
                    }
                }
            });
        });
    }
});


</script>